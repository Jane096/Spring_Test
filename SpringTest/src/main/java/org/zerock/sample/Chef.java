package org.zerock.sample;

import org.springframework.stereotype.Component;
import lombok.Data;

@Component //스프링에서 객체(bean)로 만들어 관리하는 대상임을 명시(@componentScan이 조사함)
@Data
public class Chef {
	
}
